{% extends 'base.html.twig' %}

{% block title %}Ma réservation - Travel With Your Bike{% endblock %}

{% block content %}

    <h1>Ma réservation {{ order.reference }}</h1>

    <p>
        <a href="{{ path('account_order') }}">Retour</a>
    </p>
    <hr>

    <strong> Réservation passée le : </strong>{{ order.createdAt|date('d/m/Y') }}<br>

    <strong>Référence : </strong><small> {{ order.reference }}</small><br>
    <strong>Détail de la réservation :</strong> <br>
    <table class="table mt-4">
        <thead>
        <tr>
            <th scope="col">Trajet</th>
            <th scope="col">Accompte</th>
            <th scope="col">Reste à payer </th>
            <th scope="col">Moto</th>
            <th scope="col">Nombre de passager</th>
        </tr>
        </thead>
        <tbody>
        {% for product in order.orderDetails  %}
            <tr>
                <td>{{ product.ride }}</td>
                <td>{{ (product.price / 100)|number_format(2, ',','.') }} €</td>
                <td>249,99 €</td>
                <td>{{ product.myOrder.moto | raw }}</td>
                <td>{{ product.myOrder.passengers }}</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <div class="text-right">


    </div>



{% endblock %}

